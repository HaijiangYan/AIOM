setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: DATABASE

build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production

environments:
  production:
    NODE_ENV: production  # Available at runtime

run:
  web: node server.js